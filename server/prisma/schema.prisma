// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
	provider = "prisma-client-js"
}

datasource db {
	provider = "sqlite"
	url      = "file:./dev.db"
}

// enum UserGender {
// 	MALE
// 	FEMALE 
// }

model UserModel {
	id	Int	@id @default(autoincrement())
	name	String 
	surname	String 
	birthday	DateTime
	gender String //Поправить на enum потом. Sqlite не поддерживает male | female

	distance DistanceModel[]

	@@unique([name, surname, birthday])
}

model CompetitionModel {
	id	Int @id @default(autoincrement())
	name	String @unique()
	stages StageModel[]
}

model StageModel {
	id Int @id @default(autoincrement())
	name String
	date DateTime @default(now())

	competition CompetitionModel? @relation(fields: [competitionId], references: [id])
	competitionId Int

	distance DistanceModel[]
}

model DistanceModel {
	id Int @id @default(autoincrement())
	time String
	date DateTime @default(now())
	distance Int
	temp String

	stage StageModel? @relation(fields: [stageId], references: [id])
	stageId Int?

	user UserModel @relation(fields: [userId], references: [id])
	userId Int
}
